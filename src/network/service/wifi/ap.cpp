#include "ap.h"

network::module::wifi::AP* network::service::wifi::AP::ap = 0;

void network::service::wifi::AP::Start() {
    // if (!ap)
    //     ap = new network::module::wifi::AP();
    // ap->start();
}


void network::service::wifi::AP::Loop() {
    // if (ap)
    //     ap->loop();
}